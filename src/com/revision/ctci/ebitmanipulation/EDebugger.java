package com.revision.ctci.ebitmanipulation;

public class EDebugger {
    /* (n & (n-1) == 0) */
    /* This method signifies the power of 2
     * since when we take a number and minus it with 1
     * only all the previous bits are equal to 0 it will
     * become as 0 according to the test case.
     * suppose we consider that 10001000 now if we take
     * n and n-1
     * 10001000
     * 10000111 &
     * 10000000 it is not equal to zero.
     * only if we use a power of 2 we will get zero */
}